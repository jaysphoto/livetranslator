source 'https://rubygems.org'

ruby '~>3.2' # Other options: 3.2.8 (Heroku, 3.2.x latest), 3.3.7

gem 'httparty' # for dockerification
gem 'listen'
gem 'logger'
gem 'm3u8', '~> 0.6.9'
gem 'puma' # added for heroku error
gem 'rackup' # added for heroku error
gem 'ruby-openai'
gem 'sinatra'

group :development, :test do
  gem 'dotenv'
  gem 'pry'
  gem 'rspec'
  gem 'rubocop'
end
